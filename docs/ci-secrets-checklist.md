# CI Secrets Checklist

Ensure the following secrets are configured in GitHub Repository Settings -> Secrets and variables -> Actions.

- [ ] `SLACK_WEBHOOK` (Optional): For failure notifications.
- [ ] `NPM_TOKEN` (Optional): If publishing packages.
